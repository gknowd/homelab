# Example environment variables for space-on-premises stack
# Copy this file to `.env` in the same folder and fill in secure values.

# PostgreSQL credentials
POSTGRES_USER=space
POSTGRES_PASSWORD=replace_with_strong_postgres_password
POSTGRES_DB=spacedb

# MinIO credentials
MINIO_ROOT_USER=replace_with_minio_access_key
MINIO_ROOT_PASSWORD=replace_with_strong_minio_password
